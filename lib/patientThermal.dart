import "package:flutter/material.dart";

class PatientThermalHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(child: new Text("Thermal History"),);
  }
}